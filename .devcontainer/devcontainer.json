{
  "name": "Swift + Python Dev",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "swift --version && python3 --version",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  }
}
